# PyGObject (GTK 4 / Libadwaita bindings) must be installed as a system package:
#   Ubuntu/Debian: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1
#   Fedora:        sudo dnf install python3-gobject gtk4 libadwaita
#   Arch/Manjaro:  sudo pacman -S python-gobject gtk4 libadwaita

# Pure-Python dev dependencies (pip-installable):
pytest>=7.0
